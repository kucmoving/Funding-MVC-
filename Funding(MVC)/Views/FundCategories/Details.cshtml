@model Funding_MVC_.Data.FundCategory

@{
    ViewData["Title"] = "Details";
}

<h1>Details of @Model.Name</h1>

<div>
    <h4>FundCategory</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-4">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-8">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-4">
            @Html.DisplayNameFor(model => model.DefaultAmount)
        </dt>
        <dd class = "col-sm-8">
            @Html.DisplayFor(model => model.DefaultAmount)
        </dd>
    </dl>
    <!--can add more details and document photo -->
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-primary">Edit</a> 
    <a asp-action="Index" class="btn btn-warning">Back</a>
</div>
